# Implementation Plans

Generated by the `improve` skill on 2026-07-13, planned at commit `7879781`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 015 | Restore mobile viewport meta tag (site-wide, admin dashboard reported broken) | P0 | S | — | DONE (reviewed, applied, verified live in browser) |
| 001 | Google Calendar OAuth CSRF protection | P1 | S | — | DONE (3288181) |
| 002 | Scope availability + admin calendar queries to a date range | P1 | S | — | DONE (8ffb615) |
| 004 | CI pipeline (typecheck, lint, test) | P1 | S | — | DONE (2e044df) |
| 006 | Remove dead root `middleware.ts` | P1 | S | — | DONE (d84b653) |
| 003 | Cancel/reschedule GET→POST confirmation flow | P2 | M | — | DONE (bf0c9cb) |
| 005 | Money-actions test coverage (confirmDeposit, cancelBooking) | P2 | L | 004 (land CI first) | REJECTED (superseded by 019 — wider target list incl. equipment path) |
| 013 | Recurring bookings — design spike | P2 | S (spike) | — | DONE (ae43995, spike doc landed) |
| 008 | Fix stale overlap-check comment in createWalkIn | P3 | S | — | DONE (2470ec1) |
| 007 | Landing page ISR cache | P3 | S | — | DONE (6366459) |
| 009 | Admin settings page → server component | P3 | M | — | DONE (eac5e2b) |
| 010 | Dependency upgrade — staging spike (Next/React/Tailwind/ESLint) | P3 | S (spike) | 004 (land CI before any real upgrade work) | TODO |
| 011 | Equipment inventory conflict check | P3 | M | — | DONE (fbd3317, b4654df, bd860ee, 10e1641, f6047f6) |
| 012 | Quick "book again" suggestion (no full accounts) | P3 | M | — | TODO |
| 014 | Dashboard period-over-period comparison | P3 | S | — | DONE (1d2dd5b) |
| 016 | Fix equipment double-booking race condition | P1 | M | — | DONE (reviewed, applied — Option A: advisory-lock RPC; typecheck+tests green; concurrency test not run against live DB, needs a real Supabase instance to fully confirm) |
| 019 | Money-action test coverage (supersedes 005) | P1 | L | — | DONE (reviewed, applied — createBooking/confirmDeposit/cancelBooking/equipment all covered, shared supabaseMock.ts established; 70+ tests green) |
| 017 | confirmDeposit integer-centavos validation | P2 | S | — | DONE (reviewed, applied) |
| 018 | createOnsite uses configured deposit_pct | P2 | S | — | DONE (reviewed, applied) |
| 024 | Fix README's stale PayMongo description | P2 | S | — | DONE (reviewed, applied) |
| 029 | Recurring bookings — build plan spike (follow-up to 013) | P2 | S | — | BLOCKED (013a Question 4 — deposit-per-occurrence-vs-per-series still needs studio owner's answer) |
| 020 | Dashboard parallel Supabase queries | P3 | S | — | DONE (worktree-agent-a66745d165e82b4fb, reviewed) |
| 021 | Equipment usage query date-range filter | P3 | S | — | DONE (reviewed, applied; required a supabaseMock.ts fix — see notes) |
| 022 | QR upload server-side content-type validation | P3 | S | — | DONE (migration applied; extension-fix ported by hand into StudioSettingsForm.tsx since 023's split landed after the re-run agent's worktree was created — typecheck+lint green) |
| 023 | Split SettingsClient.tsx into focused components | P3 | M | — | DONE (reviewed, applied — also absorbed 028's dedup inline) |
| 025 | Refresh HANDOFF.md to current state | P3 | S | — | DONE (reviewed, applied) |
| 026 | Clean up stray .claude/worktrees clutter | P3 | S | — | DONE (reviewed — .gitignore updated, stale worktrees pruned, active ones left alone) |
| 027 | Dashboard single-pass stat aggregation | P3 | S | 020 (land first for clean diff) | DONE (reviewed, applied, typecheck green) |
| 028 | Dedup equipment mutation/reload pattern | P3 | S | 023 (may be subsumed by it) | REJECTED (subsumed — 023's executor did this dedup inline in the new EquipmentPanel.tsx) |
| 030 | Customer-facing equipment rental at booking time | P3 | M | 016 (land race fix first) | REJECTED (already shipped — spot-checked `src/components/booking/ReviewPage.tsx:60`, full flow already wired through `createBooking`'s `equipmentIds`/RPC path; the audit that produced this finding was working from a stale read) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 015 is a P0 live-bug fix reported directly by the studio owner (2026-07-20:
  "mobile dashboard on admin not showing"). No dependency on other plans —
  do it first, it's a one-line fix in `src/app/layout.tsx`.

- 005 should land after 004 so new tests are enforced by CI immediately, not just added in isolation. 019 supersedes 005 with a wider target list (includes the equipment conflict-check path) — do not run both, see 019 for reconciliation instructions.
- 027 depends on 020 landing first (both touch the dashboard query block).
- 028 may be entirely subsumed by 023 if 023's executor does the dedup inline during the component split — check 023's outcome before starting 028.
- 030 (customer-facing equipment rental) must not ship before 016 (equipment race fix) — exposing equipment selection to customer volume increases the concurrency risk 016 closes.
- 010 (dependency upgrade spike) should only turn into real upgrade work after 004 lands — a version bump on a codebase with no CI safety net is materially riskier.
- 013 (recurring bookings) is a design spike; it may produce a blocked follow-up plan (013b) pending the studio owner's answer on the deposit-per-occurrence-vs-per-series question — that's expected, not a failure of the spike.
- 011 (equipment conflict check) has a built-in Step 1 that may conclude "defer, not enough real inventory risk to justify building this" — that's a valid, expected outcome, not a failed plan.

## Findings considered and rejected

- **"Equipment admin UI missing" (originally flagged as a direction item)**: rejected — verified false. `src/app/admin/settings/page.tsx:10,172,199` already imports and calls `createEquipment`/`deleteEquipment` from `src/lib/actions/admin/equipment.ts`. The audit subagent's grep missed the import. One residual note: `updateEquipment` is imported but never called anywhere in the settings page — dead import, not worth its own plan; whoever touches that file next can remove the unused import as a drive-by.
- **PayMongo re-enablement**: not evaluated — explicitly out of scope per `HANDOFF.md`; PayMongo being unwired is a deliberate product decision, not a bug.
- **"Duplicate middleware = live security hole" (initial audit framing)**: downgraded from a security finding to tech-debt (see plan 006) after verifying via `.next/server/middleware-manifest.json` that `src/middleware.ts` (the one with auth gating) is the file Next.js actually compiles today. The root `middleware.ts` is dead, not live-dangerous — but still worth deleting to remove the landmine for future maintainers.

## Notes for whoever picks this up

- All plans assume `pnpm` as the package manager and the exact script names in `package.json` at commit `7879781` — if that's drifted, each plan's own drift check will catch it.
- P1 plans (001, 002, 004, 006) are all S-effort, LOW-risk, and independent of each other — good candidates to knock out first and in any order or in parallel across separate branches.
- Plans 010 and 013 are spikes, not implementation — their "done" state is a design/staging document, not merged code. Don't expect a diff against `src/` from them.
